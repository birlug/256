name: evaluate

on:
  schedule:
    - cron: "59 23 * * *"
      timezone: "Asia/Tehran"
  workflow_dispatch:

jobs:
  evaluate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: download challenge.b64
        run: |
          curl -fsSL -o challenge.b64 \
            https://github.com/birlug/256/releases/download/files/challenge.b64
          test -s challenge.b64
      - name: seed local images
        run: |
          docker pull python:3.12-alpine
          docker pull alpine:3.21
          docker pull gcc:14-bookworm
          docker pull golang:1.23-alpine
          docker pull rust:1.83-alpine
      - run: python3 evaluate.py
      - name: publish scoreboard
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add SCOREBOARD.md
          git diff --cached --quiet || git commit -m "update scoreboard"
          git push
